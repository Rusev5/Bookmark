<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="WindowsOfMainTheme" parent="AppTheme">
        <item name="android:textColorHint">@color/textDarkColor</item>
        <item name="colorControlNormal">@color/textUnderLineNormalColor</item>
        <item name="colorControlActivated">@color/textUnderLineActivatedColor</item>
    </style>

</resources>
